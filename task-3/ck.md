# CK

https://github.com/mauricioaniche/ck

## Описание программного анализатора

CK вычисляет метрики кода на уровне классов и методов в Java-проектах с помощью статического анализа. В настоящее время он содержит большой набор метрик, включая знаменитый CK:

* CBO (Coupling between objects): Подсчитывает количество зависимостей, которые есть у класса. Инструмент проверяет наличие любого типа, используемого во всем классе (объявление поля, возвращаемые типы методов, объявления переменных и т.д.). Высокий CBO - признак плохой модульности.

* CBO Modified (Coupling between objects): Подсчитывает количество зависимостей, которые имеет класс. Это очень похоже на исходный CBO. Однако этот показатель рассматривает связи через поля класса: если класс A имеет поле типа класса B, то есть связь между классами. Связи являются двунаправленными - если класс A ссылается на B, а B ссылается на A, это считается как одна связь.

* FAN-IN: Подсчитывает количество входных зависимостей, которые есть у класса, т.е. количество классов, использующих данны определенный класс.

* FAN-OUT: Подсчитывает количество выходных зависимостей, которые есть у класса, т.е. количество других классов, которые используют данный класс.

* DIT (Depth Inheritance Tree): Подсчитывает длину пути от класса до самого корневого класса в иерархии наследования.

* NOC (Number of Children): Подсчитывает мисло непосредственных подклассов (детей) у данного класса.

* Number of fields: Подсчитывает количество полей в классе.

* Number of methods: Подсчитывает количество методов в классе.

* Number of visible methods: Подсчитывает количество видимых методов в классе (то есть не приватных).

* NOSI (Number of static invocations): Подсчитывает количество вызовов статических методов

* RFC (Response for a Class): Подсчитывает количество уникальных методов, которые могут быть вызваны при вызове метода этого класса

* WMC (Weight Method Class) or McCabe's complexity: Подсчитывает сумму цикломатической сложности всех методов класса.

* LOC (Lines of code): Подсчитывает количество строк кода в классе.

* LCOM (Lack of Cohesion of Methods): Подсчитывает степень, в которой методы класса связаны друг с другом через использование общих полей.

* LCOM* (Lack of Cohesion of Methods): усовершенствованная версия метрики LCOM, выдает значения от 0 до 1, где 0 - идеальная связность, а 1 - отсутствие связности.

* TCC (Tight Class Cohesion): Подсчитывает процент пар public методов класса, которые связаны через использование хотя бы одного общего поля.

* LCC (Loose Class Cohesion): Похожа на TCC, но учитывает также косвенные связи (через третьи методы)

* Quantity of returns: Подсчитывает количество возвратов из методов класса.

* Quantity of loops: Подсчитывает количество циклов в классе.

* Quantity of comparisons: Подсчитывает количество сравнений в классе.

* Quantity of try/catches: Подсчитывает количество try/catch блоков в классе.

* Quantity of parenthesized expressions: Подсчитывает количество выражений в скобках в классе.

* String literals: Подсчитывает количество строковых литералов в классе.

* Quantity of Number: Подсчитывает количество числовых литералов в классе.

* Quantity of Math Operations: Подсчитывает количество математических операторов в классе.

* Quantity of Variables: Подсчитывает количество переменных в классе.

* Max nested blocks: Подсчитывает максимальную глубину вложенности блоков кода внутри класса.

* Quantity of Anonymous classes, inner classes, and lambda expressions: Подсчитывает количество анонимных классов, вложенных классов и лямбда выражений в классе.

* Number of unique words: Посчитывает количество уникальных слов в исходном коде.

* Number of Log Statements: Подсчитывает количество log-вызовов в исходном коде.

* Has Javadoc: Boolean, указывающий, есть ли в классе javadoc.

* modifiers: public/abstract/private/protected/native modifiers of classes/methods.

* Usage of each variable: Подсчитывает как часто каждая переменная использовалась внутри каждого метода.

* Usage of each field: Подсчитывает как часто каждое локальное поле использовалось внутри каждого метода. Локальное поле (local field) — это поле (атрибут), определенное непосредственно в данном классе, в отличие от унаследованных полей от родительских классов.

* Method invocations:  Подсчитывает количество прямых вызовов методов.

## Значения метрик

### Классовые метрики

**TaggedLogger**

Метрика | Значение метрики | Анализ |
| --- | --- | --- |
CBO | 8 | Умеренное значение |
CBO modified | 12 | Умеренное значение |
FAN-IN | 4 | Низкий показатель, что хорошо, так как снижается риск наршения контрактов при рефакторинге |
FAN-OUT | 8 | Соответсвует CBO |
WMC | 150 | Крайне высокое значение метрики - тревожный сигнал. Значение 150 указывает на God Object, такой класс тяжело тестировать, поддерживать, понимать |
DIT | 1 | Простая плоская иерархия |
NOC | 0 | Нет потомков |
RFC | 14 | Очень низкое значение для класса с 55 методами, хороший знак - методы независимы |
LCOM* | 0.7064935 | Указывает на умеренно низкую связность. Методы класса сгруппированы в несколько несвязных между собой наборов, работают с разными полями. Гипотеза о нарушении принципа единственной ответственности     |
TCC | 0.81825036 | Высокий показатель, говорит о том, что методы активно используют общие поля класса, что хорошо |
LCC | 0.96153843 | Высокий показатель, говорит о том, что методы активно используют общие поля класса, что хорошо |
Число методов | 55 | Слишком большое число методов. Нарушение принципа единственной ответственности |
Число видимых методов | 52 | Вероятно, говорит о том, что класс предоставляет внешнее API |
Число полей | 14 | Нормальное количество, соизмеримое с размером класса |
LOC | 382 | В сочетании с числом методов говорит о том, что методы небольшие, проблема не в длине методов, а в количестве |

**JsonWriter**

Метрика | Значение метрики | Анализ |
| --- | --- | --- |
CBO | 10 |  Высокое значение, такие тесные связи между классами делают систему хрупкой - изменения в одном из классов могут потребовать изменений в другом |
CBO modified | 15 | Высокое значение, такие тесные связи между классами делают систему хрупкой - изменения в одном из классов могут потребовать изменений в другом |
FAN-IN | 5 | Умеренный показатель |
FAN-OUT | 10 | Соответсвует CBO |
WMC | 53 | Высокое значение, вероятно, класс труден для тестирования и понимания |
DIT | 3 | Умеренная глубина наследования |
NOC | 0 | Нет наследников |
RFC | 54 | Очень высокое значение - при вызове метода может произойти длинная цепочка вызовов других методов. Такой класс сложно тестировать |
LCOM* | 0.834375 | Крайне высокое значение, говорит о том, что методы класса почти не используют общие поля, явное нарушение принципа единственной ответственности |
TCC | 0.46666667 | Низкое значение, подтверждается гипотеза LCOM*, что методы не связаны между собой |
LCC | 0.6666667 | Низкое значение, подтверждается гипотеза LCOM*, что методы не связаны между собой |
Число методов | 16 | Адекватное число методов |
Число видимых методов | 6 | Адекватное число видимых методов |
Число полей | 20 | Высокое количество полей для 16 методов, что подтверждает гипотезу о низкой связности |
LOC | 236 | Адекватная длина для класса с 16 методами (10-15 строк на метод) |


## Сравнение метрик в зависимости от версии проекта

### TaggedLogger

| Версия | CBO | CBO modified | FAN-IN | FAN-OUT | WMC | DIT | NOC | RFC | LCOM* | TCC | LCC | Число методов | Число видимых методов | Число полей | LOC |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
2.0 | 5 | 9 | 4 | 5 | 98 | 1 | 0 | 6 | 0.5 | 1 | 1 | 52 | 51 | 8 | 261
2.8 | 8 | 12 | 4 | 8 | 150 | 1 | 0 | 14 | 0.7064935 | 0.81825036 | 0.96153843 | 55 | 52 | 14 | 382 |
3.0 | 10 | 18 | 8 | 10 | 104 | 2 | 0 | 18 | 0.7421384 | 0.9660633 | 1 | 53 | 52 | 9 | 314

В версии 1.0 класса не было.

#### Эволюция от 2.0 к 2.8

В версии 2.0 TaggedLogger - достаточно крупный класс (52 метода, 261 LOC), но с отличными показателями внутренней связности (TCC и LCC). Это означает, что все методы класса были тесно связаны между собой и использовали общие поля.

Однако к версии 2.8 наблюдается резкий рост практически всех метрик, связанных с размером и сложностью:

* WMC: с 98 до 150 (+53%). Это критически высокое значение указывает на то, что сами методы стали значительно сложнее, с большим количеством ветвлений

* LOC: с 261 до 382 строк (+46%)

* Увеличилось количество полей: с 8 до 14 (+75%), что говорит о накоплении внутреннего состояния

* Возросла связность с другими классами (CBO): с 5 до 8

При этом ухудшились метрики внутренней связности:

* LCOM* вырос с 0.5 до 0.71, что указывает на снижение связности методов

* TCC упала с 1.0 до 0.82, что означает, что в классе появились группы методов, которые не взаимодействуют друг с другом, работая с разными подмножествами полей

#### Эволюция от 2.8 к 3.0

Ключевые улучшения:

* Существенное снижение сложности: WMC уменьшился со 150 до 104 (-31%). 

* Сокращение объема кода: LOC снизился с 382 до 314 (-18%)

* Улучшение внутренней связности: TCC выросла с 0.82 до 0.97, а LCC достигла идеальной 1.0. Это говорит о том, что методы класса стали лучше взаимодействовать друг с другом, используя общее состояние

* Упрощение внутреннего состояния: Количество полей сократилось с 14 до 9 (-36%).

Компромиссы и изменения архитектуры:

* CBO увеличился с 8 до 10, а CBO modified — с 12 до 18. Функциональность, которя была внутри класса, могла быть вынесена, заменив внутреннюю сложность на внешние зависимости

* Появление иерархии наследования: DIT увеличился с 1 до 2

Общий вывод:

* Версия 2.0: Класс с хорошим дизайном

* Версия 2.8: В процессе добавления новой функциональности класс потерял четкие архитектурные границы, выросли все метрики сложности

* Версия 3.0: После проведенного рефакторинга уменьшилась сложность класса, но увеличилась связность с другимии частями системы, что является обоснованным архитектурным решением
